spring:
  datasource:
    username: postgres
    password: postgres
    url: jdbc:postgresql://localhost:5433/onlyBook
    driver-class-name: org.postgresql.Driver
    jpa:
      hibernate:
        ddl-auto: update
  rest:
    base-url: http://localhost:8080/onlyBook/api
    post-path: post
